# Database Configuration
# DB_HOST: Service name of the database container 
# (use 'db' for Docker, 'localhost' for local MySQL)
DB_HOST=db

# DB_NAME: Name of the MySQL database to create/use
DB_NAME=cookery_corner_database

# DB_USER: MySQL username (not root - create a specific user for this app)
DB_USER=cookery_user

# DB_PASS: MySQL password for the above user
DB_PASS=your_secure_password_here

# Root password used by Docker to initialize MySQL (not by PHP)
MYSQL_ROOT_PASSWORD=change_me_root

# On in dev, Off in production
PHP_DISPLAY_ERRORS=On/Off